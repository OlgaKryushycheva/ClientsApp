@model IEnumerable<ClientsApp.Models.Entities.Client>

@{
    ViewData["Title"] = "Клієнти";
    Layout = "_Layout";
}
<h2>@ViewData["Title"]</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Додати клієнта</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Ім'я</th>
            <th>Адреса</th>
            <th>Email</th>
            <th>Телефон</th>
            <th>Дії</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var client in Model)
        {
            <tr>
                <td>@client.Name</td>
                <td>@client.Address</td>
                <td>@client.Email</td>
                <td>@client.Phone</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@client.ClientId" class="btn btn-sm btn-warning">Редагувати</a>
                    <a asp-action="Delete" asp-route-id="@client.ClientId" class="btn btn-sm btn-danger">Видалити</a>
                </td>
            </tr>
        }
    </tbody>
</table>